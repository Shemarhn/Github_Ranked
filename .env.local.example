# GitHub Ranked - Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ==============================================================================
# GitHub API Configuration
# ==============================================================================

# GitHub Personal Access Token(s)
# Generate at: https://github.com/settings/tokens
# Required scopes: read:user, repo (for contribution stats)
# You can add multiple tokens for rate limit pooling (GITHUB_TOKEN_1, GITHUB_TOKEN_2, etc.)
GITHUB_TOKEN_1=ghp_your_github_token_here
# GITHUB_TOKEN_2=ghp_optional_second_token
# GITHUB_TOKEN_3=ghp_optional_third_token

# ==============================================================================
# Upstash Redis Configuration (Required for Caching)
# ==============================================================================

# Upstash Redis REST URL
# Get from: https://console.upstash.com/ or Vercel Marketplace
# This is the HTTP-based REST API URL for Edge Runtime compatibility
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io

# Upstash Redis REST Token
# Get from: https://console.upstash.com/ or Vercel Marketplace
# This is the authentication token for the REST API
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Node Environment (development, production, test)
NODE_ENV=development

# ==============================================================================
# Notes
# ==============================================================================
#
# 1. NEVER commit .env.local to version control
# 2. For production deployment on Vercel:
#    - Add these variables in the Vercel dashboard (Settings → Environment Variables)
#    - Upstash Redis can be added via Vercel Marketplace for automatic integration
# 3. Multiple GitHub tokens improve rate limits (5,000 points/hour per token)
# 4. Upstash Redis uses HTTP REST API (not TCP) for Edge Runtime compatibility
